<form method="post" action="{{ path('profil_delete', {'id': profil.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer ce profil ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil.id) }}">
    <button class="btn_register">Supprimer</button>
</form>
